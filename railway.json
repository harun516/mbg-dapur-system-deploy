{
    "build": {
        "builder": "nixpacks",
        "buildCommand": "rm -f public/hot && composer install --optimize-autoloader --no-dev && npm ci && npm run build && php artisan storage:link"
    },
    "deploy": {
        "startCommand": "bash start.sh"
    }
}
