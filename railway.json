{
    "build": {
        "builder": "nixpacks",
        "buildCommand": "composer install --optimize-autoloader --no-dev && npm ci && npm run build && php artisan storage:link && mkdir -p /tmp/nginx && cp nginx.conf /tmp/nginx/"
    },
    "deploy": {
        "startCommand": "bash start.sh"
    }
}
